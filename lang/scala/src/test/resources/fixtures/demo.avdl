@namespace("org.apache.avro.scala.generated")
protocol Org {
  enum Species {
    DOG, HAMSTER
  }

  record Pet {
    Species species;
  }

  record Person {
    string name;
    int age;
    map<Pet> pets;
    union { string, double } onion;
  }
}
